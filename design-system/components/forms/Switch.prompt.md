Boolean toggle — blue track when on, white knob on a soft shadow. Controlled via `checked` + `onChange`.

```jsx
<Switch checked={readOnly} onChange={setReadOnly} label="Только просмотр" />
<Switch checked={grid} onChange={setGrid} size="sm" />
```
