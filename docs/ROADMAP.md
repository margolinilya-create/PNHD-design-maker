# PINHEAD — дорожная карта

Живой бэклог сверх `BUILD.md`. Порядок: **лекала + точность превью** → паритет с Printful.

## Сделано
- MVP (каркас, геометрия §4 + тесты, редактор react-konva, экспорт векторного PDF 1:1).
- Реальный per-size регрейдинг (константа отступа от горловины) на `View.size_anchors`.
- Раунд точности/UX: per-size якоря в PDF, жёсткая zod-валидация, чтение DPI из PNG (pHYs),
  zoom/pan, клавиши, ручной ввод мм, сетка 50 мм, горизонталь «от центра».
- Инструменты проверки точности: калибровочная шкала 100 мм в PDF, «Линейка» на холсте,
  оверлей «Проверка лекала» (якоря/оси/краевые линейки 50 мм).

## Трек «лекала + точность» (текущий приоритет)

### P0 — калибровка масштаба ✅
- `scale_mm_per_unit` задействован сквозняком: на холсте (flatMm = naturalWidth × scale)
  и в экспорте PDF (флэт обёрнут в `<g transform="scale(s)">`, страница в мм).
- Калибровка в «Линейке»: измеряешь известный элемент → вводишь реальную мм →
  получаешь `scale_mm_per_unit` для записи в лекало (skus.json).
- Тест: PDF масштабирует флэт и держит страницу в мм.

### P1 — точность данных
- **Per-size зоны печати** (`print_areas` градуируются по ростовкам, а не только якоря).
  Модельная доработка: зоны как функция размера.
- **Ручная калибровка растрового макета** (ввод реальной ширины мм / DPI вместо дефолта 120 мм).
- **Live-индикатор DPI** (решено: бейдж Good/Средне/Низко = пиксели макета / реальный размер
  печати; НЕ блокирует) — `SidePanel`/оверлей.

### Заведение первого реального SKU
- Пайплайн DXF/AI/PDF → SVG в мм → калибровка → якоря → зоны → ростовки.
- Эталон для регрессий точности.

## После реального лекала — паритет с Printful (решения зафиксированы)

> Делаем ПОСЛЕ P0/P1 и заведения реального лекала.

- **Зоны нанесения — конфиг под каждый SKU** (в лекальной базе, не хардкод):
  оба рукава (`sleeve_left`/`sleeve_right`), этикетки шеи (inside/outside label),
  **несколько зон на одном виде** (сейчас берётся только `print_areas[0]` — снять это ограничение
  в `viewZone`, `EditorCanvas`, `SidePanel`, `buildSceneSvg`; placement уже несёт `print_area_id`).
- **Маскирование макета по зоне + предупреждение** (решено): визуально обрезать всё, что вне
  печатной зоны (как Printful), и сохранять текстовое предупреждение об выходе.
- **Пресеты позиции** (решено): «центр по горизонтали», «в центр зоны», «вверх/вниз» —
  быстрое выравнивание; контракт стора `updatePlacement` готов.

## Вне MVP (BUILD.md §12)
3D · жёсткий DPI-блок · ручной регрейдинг per-size · интеграции CRM ·
маркетинговые мокапы (вкладка Mockups у Printful) — мы делаем тех-рисунок, не мокап ·
Crop макета и Pattern/плитка (all-over) — отложены, к приоритету не отнесены.
